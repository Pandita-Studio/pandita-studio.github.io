---
const { type, id, name, ...rest } = Astro.props;
---

<input
	class="border-2 border-gray-300 outline-pink-500 hover:border-pink-500 hover:text-pink-500 focus:text-pink-500 transition-colors duration-300 rounded px-2 py-1 disabled:outline-0 disabled:hover:border-gray-300"
	type={type}
	id={id}
	name={name}
	{...rest}
/>
